<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">Receipt-Symf
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">


				<li class="nav-item active">
					<a class="nav-link" href="/">Acceuil
						<span class="visually-hidden"></span>
					</a>
				</li>

				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{path('ingredient.index')}}">Mes ingredients
							<span class="visually-hidden">(current)</span>
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{path('receipt.index')}}">Mes Recettes
							<span class="visually-hidden">(current)</span>
						</a>
					</li>
				{% endif %}
			</ul>
			{% if app.user %}
				<div class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{app.user.fullName}}</a>
					<div class="dropdown-menu">
						<a class="dropdown-item" href="{{path('user.edit', {id : app.user.id})}}">Modifier mes informations personneles</a>
						<a class="dropdown-item" href="{{path('user.edit.password', {id : app.user.id})}}">Modifier mot de passe</a>
						{% if 'ROLE_ADMIN' in app.user.roles %}
							<a class="dropdown-item" href="{{path('admin')}}">Adminsitration</a>
						{% endif %}
						<div class="dropdown-divider"></div>
						<a class="dropdown-item" href="{{path('security.logout')}}">Deconnexion</a>
					</div>
				</div>
			{% else %}
				<div class="d-flex justify-content-between">
					<a class="nav-link mx-3 text-light" href="{{path('security.login')}}">Connexion

					</a>
					<a class="nav-link text-light" href="{{path('security.registration')}}">Inscription

					</a>
				</div>


			{% endif %}


		</div>
	</div>
</nav>
